# AI Traffic Management System - Runtime dependencies
#
# CPU-only installs (recommended for servers without an NVIDIA GPU) should
# install torch from the CPU wheel index first, otherwise pip pulls ~3 GB of
# CUDA runtime libraries:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Development / test tooling lives in requirements-dev.txt.

# --- Web framework -----------------------------------------------------------
fastapi~=0.140.0
uvicorn[standard]~=0.51.0
python-multipart~=0.0.20
websockets~=15.0

# --- Computer vision / inference ---------------------------------------------
ultralytics~=8.4.0
opencv-python-headless~=4.12.0
torch>=2.6.0
torchvision>=0.21.0
numpy>=2.1.0,<3
Pillow~=12.0

# --- Validation & settings ---------------------------------------------------
pydantic~=2.13.0
pydantic-settings~=2.14.0

# --- Persistence -------------------------------------------------------------
SQLAlchemy[asyncio]~=2.0.51
aiosqlite~=0.22.0
redis~=6.0

# --- HTTP client & async IO --------------------------------------------------
httpx~=0.28.0
aiofiles~=24.1.0

# --- Observability -----------------------------------------------------------
structlog~=25.4
prometheus-client~=0.23.0
psutil~=7.0

# --- Configuration & security ------------------------------------------------
python-dotenv~=1.1
PyJWT~=2.10
cryptography>=44.0.0

# --- Performance -------------------------------------------------------------
orjson~=3.11.0

# --- Production server -------------------------------------------------------
gunicorn~=23.0
